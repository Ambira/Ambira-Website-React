// src/components/Header.jsx
import React from "react";
import Navbar from "./Navbar";

const Header = () => {
  return (
    <header>
      <Navbar />
      {/* You can add additional header content (logo, banner, etc.) here if needed */}
    </header>
  );
};

export default Header;
